<?php

namespace {{ name_space }}\Form;

use PrestaShop\PrestaShop\Core\Configuration\DataConfigurationInterface;
use PrestaShop\PrestaShop\Core\Form\FormDataProviderInterface;

class {{ class_name }}FormDataProvider implements FormDataProviderInterface
{
    /** @var DataConfigurationInterface */
    private $configurationData;

    public function __construct(DataConfigurationInterface $configurationData)
    {
        $this->configurationData = $configurationData;
    }

    /**
     * {@inheritDoc}
     */
    public function getData()
    {
        return $this->configurationData->getConfiguration();
    }

    /**
     * {@inheritDoc}
     */
    public function setData(array $data)
    {
        return $this->configurationData->updateConfiguration($data);
    }
}
